import 'package:flutter/material.dart';

class SubjectForm extends StatelessWidget {
  const SubjectForm({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Cadastro de Matérias'),),
    );
  }
}